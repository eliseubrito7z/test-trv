export const queryActions = ["where"];
export const readActions = ["include", "select"];
export const writeActions = [
    "create",
    "update",
    "upsert",
    "createMany",
    "updateMany",
    "delete",
    "deleteMany",
    "disconnect",
    "connect",
    "connectOrCreate",
];
export const toOneRelationNonListActions = [
    "create",
    "update",
    "delete",
    "upsert",
    "connect",
    "connectOrCreate",
    "disconnect",
];
export function isQueryAction(action) {
    return queryActions.includes(action);
}
export function isReadAction(action) {
    return readActions.includes(action);
}
export function isWriteAction(action) {
    return writeActions.includes(action);
}
export const modifiers = ["is", "isNot", "some", "none", "every"];
export const logicalOperators = ["AND", "OR", "NOT"];
